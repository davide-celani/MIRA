% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mira_fit.R
\name{mira_fit}
\alias{mira_fit}
\title{Fit MIRA longitudinal treatment model}
\usage{
mira_fit(
  stan_data,
  prior = NULL,
  chains = 4,
  parallel_chains = chains,
  iter_warmup = 2000,
  iter_sampling = 3000,
  seed = 123,
  refresh = 100,
  adapt_delta = 0.95,
  step_size = 0.1,
  max_treedepth = 12,
  metric = c("diag_e", "dense_e", "unit_e"),
  show_messages = TRUE,
  stan_file = NULL,
  verbose = TRUE,
  rhat_threshold = 1.01,
  ess_threshold = 400,
  ebfmi_threshold = 0.3,
  treedepth_tolerance = 0.01
)
}
\arguments{
\item{stan_data}{Data prepared for the MIRA Stan model. The list may
contain additional R-side metadata (for example \code{mean_y}, \code{sd_y}, or
\code{arm_labels}, gender labels, or the age threshold); only variables required
by Stan are passed to CmdStan. The current model requires subject-level
binary indicators \code{male} (0 = Female reference, 1 = Male) and
\code{age_above_threshold} (0 = age <= threshold, 1 = age > threshold).}

\item{prior}{A \code{mira_prior} object, or a named list containing the Stan
prior fields required by the selected model, including dedicated priors
for gender- and age-threshold trajectories. If \code{NULL}, \code{mira_prior()}
automatically selects the outcome and uses standard priors, unless all
prior fields are already present in \code{stan_data}. A non-NULL \code{prior} takes
precedence over prior fields embedded in \code{stan_data}.}

\item{chains}{Number of MCMC chains.}

\item{parallel_chains}{Number of parallel chains.}

\item{iter_warmup}{Number of warmup iterations.}

\item{iter_sampling}{Number of sampling iterations.}

\item{seed}{Random seed.}

\item{refresh}{Number of iterations between progress messages.}

\item{adapt_delta}{Target average acceptance probability used during NUTS
adaptation. Values closer to one reduce integration error and can remove
divergent transitions, at the cost of additional computation.}

\item{step_size}{Positive initial integrator step size. It is adapted during
warmup. A conservative default is used to avoid invalid first-iteration
proposals for positive scales and Cholesky factors.}

\item{max_treedepth}{Maximum NUTS tree depth.}

\item{metric}{Euclidean metric used by NUTS: \code{"diag_e"}, \code{"dense_e"}, or
\code{"unit_e"}. The default diagonal metric is appropriate for this
non-centred hierarchical model.}

\item{show_messages}{Logical. If \code{TRUE}, retain CmdStan informational
messages, including rejected warmup proposals.}

\item{stan_file}{Optional path to the Stan file. If \code{NULL}, MIRA first
looks for \code{inst/stan/gaussian_longitudinal.stan}, then legacy MIRA Stan
filenames for backwards compatibility.}

\item{verbose}{Logical. If TRUE (default), print a compact MIRA fit report
automatically after successful sampling. The fitted CmdStanMCMC object is
still returned invisibly and can be assigned normally.}

\item{rhat_threshold}{Upper diagnostic threshold for rank-normalized R-hat.
Values below this threshold pass the automatic fit check.}

\item{ess_threshold}{Minimum acceptable bulk and tail effective sample size.}

\item{ebfmi_threshold}{Lower diagnostic threshold for E-BFMI.}

\item{treedepth_tolerance}{Proportion of post-warmup transitions allowed to
hit \code{max_treedepth} before the report raises an efficiency warning. A
positive proportion not exceeding this value is labelled \code{MINOR}: it does
not by itself make the posterior fit invalid.}
}
\value{
A CmdStanMCMC object.
}
\description{
Fits the outcome-adaptive MIRA longitudinal mixed-effects model with
treatment-, gender-, and age-threshold-specific trajectories using CmdStan.
The likelihood and link are selected by \code{\link[=mira_prepare_data]{mira_prepare_data()}}: censored
Student-t/Gaussian identity models and a positive log-normal/log-link model
share a coherent natural-scale clinical output interface.
}
