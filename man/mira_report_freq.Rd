% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mira_report_freq.R
\name{mira_report_freq}
\alias{mira_report_freq}
\title{Create a dynamic, exhaustive Quarto report from mira_info()}
\usage{
mira_report_freq(
  x = NULL,
  data = NULL,
  ...,
  output_dir = NULL,
  output_file = "mira_report",
  format = c("html", "pdf"),
  title = "MIRA: analisi longitudinale",
  subtitle = "Report statistico dinamico e riproducibile",
  author = NULL,
  date = Sys.Date(),
  sections = "all",
  include_complete_output = TRUE,
  extra_objects = list(),
  max_table_rows = Inf,
  max_table_columns = 12L,
  digits = 3L,
  max_depth = 50L,
  render = TRUE,
  open = interactive(),
  overwrite = FALSE,
  quiet = TRUE
)
}
\arguments{
\item{x}{An existing \code{mira_info}, \code{mira_info_multi}, or \code{mira_detect} object.
A data.frame is also accepted and is treated as \code{data}.}

\item{data}{Optional data.frame. If supplied, \code{mira_info(data, ...)} is run first.}

\item{...}{Arguments forwarded unchanged to \code{mira_info()} when \code{data} is used.}

\item{output_dir}{Directory in which the Quarto source, RDS payload, runtime
helper, and rendered reports are written. The default creates a timestamped
directory under the current working directory.}

\item{output_file}{Base filename without extension.}

\item{format}{One or more of \code{"html"}, \code{"pdf"}, \code{"docx"}, or \code{"all"}.}

\item{title, subtitle, author, date}{Report metadata.}

\item{sections}{\code{"all"} or any subset of the documented report sections.}

\item{include_complete_output}{If TRUE, append a recursive rendering of the
complete result object. This is TRUE by default to avoid silent omissions.}

\item{extra_objects}{Optional named list of additional fitted objects (for
example \code{list(mira_fit = fit)}) to inventory and include in the appendix.}

\item{max_table_rows}{Maximum rows printed per table. Defaults to Inf: there is
no arbitrary row or page limit. A finite value must be explicitly requested.}

\item{max_table_columns}{Maximum columns per displayed block. Wide tables are
split into blocks; columns are never dropped.}

\item{digits}{Number of display digits.}

\item{max_depth}{Safety depth for recursive object traversal.}

\item{render}{If TRUE, render with Quarto; if FALSE, only create reproducible
\code{.qmd}, \code{.R}, and \code{.rds} source files.}

\item{open}{Open the first rendered report in an interactive session.}

\item{overwrite}{Allow existing source or output files to be replaced.}

\item{quiet}{Passed to \code{quarto::quarto_render()}.}
}
\value{
Invisibly, an object of class \code{mira_report_freq} containing the MIRA
result and all generated paths.
}
\description{
Create a dynamic, exhaustive Quarto report from mira_info()
}
