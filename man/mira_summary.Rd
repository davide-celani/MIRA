% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mira_summary.R
\name{mira_summary}
\alias{mira_summary}
\title{Research-oriented summary of a MIRA Bayesian fit}
\usage{
mira_summary(
  fit,
  stan_data = NULL,
  meaningful_change = NULL,
  y = NULL,
  credible_level = 0.9,
  responder_thresholds = c(0.5, 0.8, 0.95),
  verbose = TRUE
)
}
\arguments{
\item{fit}{A fitted CmdStanMCMC object.}

\item{stan_data}{Optional data list used to fit the model. Supplying it is
strongly recommended because it provides arm membership, time values,
direction of improvement and clinical thresholds.}

\item{meaningful_change}{Optional fixed MCID fallback for backwards
compatibility. In the current model the posterior draws of \code{mcid} are
used whenever available.}

\item{y}{Optional observed outcome vector. If NULL, it is recovered from
\code{stan_data}.}

\item{credible_level}{Width of posterior credible intervals.}

\item{responder_thresholds}{Posterior probability thresholds used for
existing-subject responder classification.}

\item{verbose}{Logical. If TRUE, print the compact Bayesian report when
the summary object is created.}
}
\value{
A list containing population trajectories, changes, new-subject
responder estimands, individual summaries, treatment contrasts,
time-specific gender and age-threshold contrasts (with group sizes when
\code{stan_data} is supplied), heterogeneity,
posterior predictive checks, log-likelihood information,
MCMC diagnostics, model information and raw posterior draws.
}
\description{
Summarizes the outcome-adaptive multi-arm MIRA longitudinal model. The
function is dynamic in likelihood/link, treatment arms G, measurement
occasions K and subjects S. Natural-scale outcomes and clinical changes are
reported for every family; log-scale and ratio estimands are additionally
exposed for log-normal CMT fits.
}
